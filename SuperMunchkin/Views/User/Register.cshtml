
@{
    ViewData["Title"] = "Register";
}

@model SuperMunchkin.ViewModels.RegisterUserViewModel

<div class="w-100 text-center mt-5">
    <h1>SuperMunchkin</h1>
</div>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div style="height: 70vh;">
        <div class="text-center">
            <div class="w-100" style="height: 50px;">
                @ViewBag.ErrorMessage
            </div>
            <div class="mb-1 pt-5">
                @Html.TextBoxFor(m => m.Username, new { placeholder = "Username", @class = "custom-border p-1" })
            </div>
            <div class="mb-1">
                @Html.PasswordFor(m => m.Password, new { placeholder = "Password", @class = "custom-border p-1" })
            </div>
            <div class="mb-1">
                @Html.PasswordFor(m => m.PasswordCheck, new { placeholder = "Repeat Password", @class = "custom-border p-1" })
            </div>
            <div class="mb-1">
                @Html.TextBoxFor(m => m.Email, new { placeholder = "Email", @class = "custom-border p-1" })
            </div>
            <div>
                <button class="button-big custom-border mr-3">
                    @Html.ActionLink("Cancel", "Login", "User")
                </button>
                <input type="submit" value="Register" class="button-big custom-border" />
            </div>
        </div>            
    </div>
}